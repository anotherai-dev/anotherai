export function AuthProvider({ children }: { children: React.ReactNode }) {
  //     <ClerkProvider
  // signInFallbackRedirectUrl="/"
  //           afterSignOutUrl="/"
  //           signInUrl="/sign-in"
  //           signUpUrl="/sign-up"
  //           />

  return children;
}
